const EMPTY_JSON = "{}";

enum FILE_DIRECTORIES {
  SYSTEM_CONFIG_DIR = ".config",
  TERMINAL_ASSISTANT_CONFIG_DIR = "terminal-assistant",
}

const FILE_ENCODING = "utf-8";

enum STORAGE_FILE_NAMES {
  FLOWS = "flows.json",
}

export { EMPTY_JSON, FILE_DIRECTORIES, FILE_ENCODING, STORAGE_FILE_NAMES };
